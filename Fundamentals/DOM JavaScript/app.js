// window object
let value;
/*value = document;
value = document.all;
value = document.all[0];

value = document.all.length;
value = document.all[document.all.length - 1];
/*const elements = document.all; //html colection
for (let i = 0; i < elements.length; i++) {
  console.log(elements[i]);
}
// forEach de arraye cevirmemiz lazim

elements.elementforEach(function (element) {
  console.log(element);
});
const colections = Array.from(document.all);
colections.forEach(function (colection) {
  console.log(typeof colections);
});*/
value = document.all[8];
value = document.body;
value = document.head;
value = document.location;
value = document.location.hostname;
value = document.URL;
value = document.characterSet;

console.log(value);
